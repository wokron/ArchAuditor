以下是根据 PDF 内容转换的 Markdown 格式文档：

# 云原生性能架构评估 —— ArchAuditor

*   可观测性：日志、指标、链路追踪、性能剖析……
*   可观测性基础架构已经十分完善。
*   利用观测数据可以实时获取微服务系统的运行状态，定位性能等问题。**相较之下，架构问题则更加难以定义，难以识别。**没有“什么是优秀的架构”的指导，系统十分容易出现架构退化等问题，积累隐形的技术债务。
*   **因此：**提出一种方法，以观测数据为基础，构建架构评估模型，对云原生微服务系统的架构进行持续监控和分析。实现架构退化问题的及时定位。

## 项目整体思路

1.  **基于服务等级的架构检测方法**
    *   架构问题难以识别，关键在于仅凭观测数据，难以了解系统的设计目标。ArchAuditor 引入了 **服务等级标记机制**。用户可预先标记服务的优先等级。ArchAuditor 会根据系统的实际表现，查看服务级别是否被违反，从而实现“架构漂移”的检测，定位架构与实现不一致的位置。
    *   **举个例子：** A 服务为 P0 级别，B 服务为 P1 级别。通过分析运行时数据发现，A 需要依赖 B 的接口，且 B 的不可用会导致 A 的同样不可用。这样就造成了高优先级任务对低优先级任务的依赖。
    *   通过服务等级，ArchAuditor 还可以区别对待不同服务的配置状态，对更高等级的服务采取更加严格的检查，从而更加精确的实现问题的识别。**例如：** P0 级别的任务需要赋予更强的可靠性保障，因此需要提供更强的副本和物理隔离手段。

2.  **多源异构观测数据的融合分析**
    *   架构问题无法通过单类型的观测数据进行识别，必须结合多种数据进行分析。**例如：**为了识别前面的问题，需要依赖 “服务间依赖关系” “依赖关系强弱（通过错误率等指标识别）” 和 “服务等级标注” 等数据。
    *   框架需要同时支持 “多种数据来源” 和 “多种分析目标”。简单的数据处理程序难以实现这一目标。
    *   ArchAuditor 提供了 Processor + SystemState 的抽象。SystemState 用于表示目标系统状态，而 Processor 则用于实现 “获取观测数据以更新系统状态“ 和 ”根据系统状态进行分析“ 两个目标。通过定义 Processor 之间的依赖关系并进行执行调度，ArchAuditor 提供了强大的架构分析表达能力。

## ArchAuditor

目前已经实现的基本框架，需要各位在此基础上进行开发，实现：

*   **数据接入：**与各类可观测性平台对接，实现观测数据的获取
*   **前端界面：**
    *   **控制面板：**服务提供的主页，用于提供手动触发检查操作，修改调度逻辑，查看报警信息等等操作。
    *   **分析结果展示：**提供分析结果的可视化展示。需要实现图的可视化。例如，标记图中的循环。
    *   （我会实现一些分析算法，各位也可以给出补充）
*   **Processor 组件：**
    *   ArchAuditor 中所有可定制部分都是一个 Processor。Processor 之间存在依赖关系，他们都可以访问 SystemState 数据结构。
    *   一次 Audit 过程，所有 Process 都会按其依赖关系依次执行。
    *   一部分 Processor 用于从某处获取最新的观测数据，并据此修改 SystemState，称为 **Source**。
    *   另一部分 Processor 用于查看并分析 Source 更新后的 SystemState，得到某些审计结果。称为 **Analyzer**。
*   **前端：**
    *   ArchAuditor 单个服务提供所有功能，包括前端
    *   前端通过 **FastAPI** 返回 HTMLResponse 实现。HTMLResponse 通过 **jinja** 基于模板生成。

## 预期要识别的架构问题

先从 4 个架构问题开始：

1.  **资源规范性问题：** k8s 资源配置不合理，容易出现安全或可靠性问题
2.  **循环依赖：** 服务之间存在循环的调用链路
3.  **违反依赖关系：** 高等级的服务强依赖于低等级的服务，未实现降级
4.  **单点故障：** 某个服务是其他大部分服务的必经节点，且/或服务未能提供副本等可靠性保障配置。

## 分工

*   **数据接入**
    *   k8s 资源配置
    *   服务间的依赖关系（服务拓扑）
    *   各服务的请求错误率时序数据
    *   各服务的请求时延时序数据
    *   各服务的请求流量（QPS 信息）

*   **前端界面**
    *   **控制面板**
        *   手动触发 audit 检查
        *   显示 report 报警结果
        *   修改各服务的等级
        *   导航到各分析的可视化界面
    *   **可视化**
        *   列表展示当前的资源规范性问题
        *   可视化展示服务拓扑，并标记其中的循环依赖
        *   可视化展示服务拓扑，并标记违反依赖关系的节点
        *   可视化展示服务的支配树，并标记有单点故障风险的节点